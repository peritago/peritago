# 백엔드 오리진. vite dev 프록시가 /api, /ws를 여기로 넘깁니다.
VITE_BACKEND_ORIGIN=http://localhost:8080

# 결과 카드 SSE 엔드포인트. 비워두면 목 스트림으로 동작합니다.
# 백엔드에 translate 도메인이 생기면 아래 줄의 주석을 해제하세요.
# VITE_TRANSLATE_STREAM=/api/translate/stream

# STT 서버 스트리밍 소켓. 비워두면 Web Speech API로 폴백합니다.
# VITE_STT_SOCKET_URL=/ws/stt
